{#
/**
 * @file jalur_prestasi.html.twig
 * Default theme implementation to present Jalur prestasi data.
 *
 * This template is used when viewing Jalur prestasi pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_jalur_prestasi()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('jalur_prestasi') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
